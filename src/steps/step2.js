'use strict';

import React from 'react';

const Step2 = (props) => (
    <div className="step step2">
      <p>Step 2</p>
      {/* alternative call for previous */}
      {/* <a href="#" onClick={() => props.jumpToStep(0)}>Back1</a>*/}
    </div>
)

export default Step2;
